platform: x64
image: Visual Studio 2015

environment:
  global:
    XGBOOST_BASE_VERSION: 0.7-criteo-20170608163439
    XGBOOST_VERSION: '%XGBOOST_BASE_VERSION%-win64'
    LIBHDFS_DIR: '%APPVEYOR_BUILD_FOLDER%\libhdfs-dev'
    HADOOP_VERSION: 2.6.0-cdh5.5.0
    SPARK_VERSION: 2.1.0
    SCALA_VERSION: 2.10.6
  matrix:
    - JAVA_HOME: C:\Program Files\Java\jdk1.8.0
    # - JAVA_HOME: C:\Program Files (x86)\Java\jdk1.8.0

clone_script:
  - git clone -q --branch=%APPVEYOR_REPO_BRANCH% https://github.com/%APPVEYOR_REPO_NAME%.git %APPVEYOR_BUILD_FOLDER%
  - cd %APPVEYOR_BUILD_FOLDER%
  - git checkout -qf %APPVEYOR_REPO_COMMIT%
  - git submodule update --init --recursive
  - git submodule foreach --recursive git pull origin master

install:
  - 'SET PATH=C:\mingw-w64\x86_64-6.3.0-posix-seh-rt_v5-rev1\mingw64\bin;%PATH%'
  - 'SET PATH=%VS140COMNTOOLS%\..\IDE;%PATH%'
  - ps: If (!(Test-Path -Path $env:LIBHDFS_DIR)) { iex 'C:\Python36-x64\python.exe install_libhdfs.py' }

build_script:
  - 'C:\Python36-x64\python.exe install_xgboost.py'

test_script:
  - cd xgboost
  - 'SET HADOOP_HOME=%CD%'
  - cd jvm-packages
  - mvn -q package -pl :xgboost4j
  # 'kryoSerializer test' in xgboost4j-spark seems to fail on Windows.
  # The issue has been reported upstream, see dmlc/xgboost#2394.
  - mvn -q package -pl :xgboost4j-spark -am -DskipTests -Dmaven.test.skip

cache:
  - '%LIBHDFS_DIR%'

artifacts:
  - path: 'xgboost\jvm-packages\xgboost4j-spark\target\xgboost4j-spark-%XGBOOST_VERSION%.jar'
  - path: 'xgboost\jvm-packages\xgboost4j\target\xgboost4j-%XGBOOST_VERSION%.jar'

deploy:
  provider: GitHub
  tag: '$(XGBOOST_BASE_VERSION)'
  description: Windows JAR for xgboost
  force_update: true
  auth_token:
    secure: 'GAsVDc+Y0V9vOCuF+W9l7RVr2r1IoOgRfuRm6Jgd3fjIgylg2C4wudvzI25D4XYD'
  on:
    branch: master
